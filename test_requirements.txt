# Test requirements for Green-Ampt QGIS Plugin
# Install with: pip install -r test_requirements.txt

# Core testing framework
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0  # For parallel test execution
pytest-html>=3.0.0   # For HTML test reports

# Mocking and fixtures
responses>=0.22.0     # For HTTP mocking
freezegun>=1.2.0      # For time mocking

# Performance and monitoring
psutil>=5.9.0         # For memory/CPU monitoring
memory-profiler>=0.60.0  # For memory profiling

# Data handling for tests
pandas>=1.5.0         # For test data manipulation
geopandas>=0.12.0     # For spatial test data
shapely>=2.0.0        # For geometry testing

# Development and debugging
ipdb>=0.13.0          # For debugging tests
pytest-benchmark>=4.0.0  # For performance testing